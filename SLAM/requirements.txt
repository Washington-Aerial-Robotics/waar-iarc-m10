# Pin NumPy 1.x for compatibility with common conda stacks (matplotlib, pyarrow, etc.)
numpy>=1.22,<2
opencv-python>=4.8,<4.12
scipy>=1.7,<1.14
matplotlib
pupil-apriltags
ultralytics>=8.0,<9

# Optional: stereo_depth.py uses pandas; mine_detection_pipeline does not require it.
pandas>=1.5,<3
